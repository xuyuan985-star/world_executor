# requirements.lock（Sprint C：精确版本冻结——防供应链漂移/投毒复发）
# 安装（带 hash 校验）：
#   pip install --require-hashes -r requirements.lock
# 重新生成 hash（联网环境）：
#   pip download -r requirements.lock -d .lockwheel && pip hash ...
# 注：March7th 集成依赖（ruamel.yaml/rapidocr/onnxruntime-directml/mss）
# 由 March7thAssistant 自带环境提供，严禁 pip 安装历史投毒包 pylnk3。
#
# --- WorldExecutor runtime 核心（本机已验证版本） ---
numpy==2.2.6
pillow==10.4.0
PyAutoGUI==0.9.54
pywin32==312
PyYAML==6.0.3
requests==2.34.2

# --- March7th 集成最小集（安装自 March7thAssistant 环境，勿单独 pip 安装） ---
# ruamel.yaml
# pyautogui
# mss
# pywin32
# rapidocr-onnxruntime
# onnxruntime-directml

# --- GUI（独立 venv） ---
# PySide6
