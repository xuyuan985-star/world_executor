# WorldExecutor 变更与问题报告

> 规则：每次修改、每个新问题都记录一节。按日期追加，最新在最上面。
> 提交后本文件与代码同步推送。

---

## 2026-08-08（第 2 轮：解冻后工程落地）

### 变更记录（commit 1dab627 → 2a67cf6）

1. **基于 March7th 原生 API 重写执行层**（1dab627）
   - `interact_template` / `click_text`（新增）改用 `auto.click_element(目标, image/text, threshold, max_retries)` 一站式链路
   - `verify_signal` 改用 `auto.find_element`
   - 点击链路全部带 `backend/success/error`（Replay 可诊断）
2. **企划 v0.12 解冻**（3c015bb）：Engineering Evolution 1.0；March7th 原生 API 执行层、坐标体系修正、集成硬事实清单（提权/cwd/后台截图/双实例/pylnk3/60s 重试坑）
3. **企划 v0.12.1 Engineering Patch + 全部落地**（2a67cf6）：
   - ActionIntent 抽象（`runtime/decision/action.py`，决策层零坐标）
   - March7th Driver 层（`runtime/drivers/march7th/{input,vision,window}`）
   - Input Health L0-L2（`runtime/health.py` 重写；L2 失败阻止 mission）
   - Emergency Pause（`runtime/safety.py`，human_intervention 事件）
   - Failure Category F1/F2/F3（GUI Failure Inspector 分类显示）
   - GUI 健康徽标分级显示（输入 L0/L1/L2）
   - schema 新增 `human_intervention` 事件类型

### 验证结果

- dry_run：4/4 PASS (logical)
- 架构检查（tools/architecture_check.py）：通过
- GUI 导入冒烟：通过

### 新问题（本次发现）

| # | 问题 | 状态 | 处置 |
|---|---|---|---|
| N1 | 游戏未启动时 March7th `take_screenshot` 空转 **60 秒**才抛错（源码：take_screenshot 循环重试上限 60s） | 已规避 | 所有调用前先 `find_game_window` 快速失败（校准脚本/health 已加） |
| N2 | March7th `get_window` 取**第一个** title 匹配窗口，双实例场景会踩中隐藏 0x0 窗口 | 已规避 | driver.window 自建可见窗口枚举 + 最大客户区选择 |
| N3 | click_test 旧实现含错误 DPI 换算（`lx × 1.25`，OCR 跑在物理截图上还乘缩放）——点击点偏移 25% 是"点击无响应"的主因之一 | 已修复 | 改用 March7th `take_screenshot` + `screenshot_pos` 体系，无乘法 |
| N4 | 分辨率变动：当前显示器 2048×1152（此前假设 1920×1080 / DPI 1.25） | 跟踪 | 由 March7th screenshot_scale_factor 归一化兜底；M1-A 实测确认 |
| N5 | 点击链路真机验证**仍未完成**：游戏窗口当前不存在（未启动），校准/提权点击均无法执行 | **阻塞** | 解除条件：用户启动游戏 → coords_calibrate.py（不弹UAC）→ click_test.py（提权） |

### 下一步

```
游戏启动 → ① coords_calibrate.py 校准 → ② click_test.py 提权点击验证 → ③ M1-A chest_001 ×3
```

---

## 2026-08-08（第 1 轮：Sprint 0 收尾，见 docs/阶段报告-20260808.md）

- 17 项会话问题已导出归档（failure_reports/20260808_185949_session_all_issues/）
- 阶段性总报告完成（b159779）
- 6 项 Review 建议（input backend / health / action_executed 字段 / temporal / inspector / 附录B 命名勘误）落地（1e451fc）
