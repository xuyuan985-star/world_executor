# WorldExecutor 运行时依赖（Part 2-2.8：锁定版本，禁止浮漂）
# 用途：dry_run / smoke / runtime 核心 / GUI / m7 任务中心（唯一环境 m7_venv）
# 安装：pip install -r requirements.txt
# 注意：m7 自身依赖（rapidocr/onnxruntime-directml/colorama 等）在
#   March7thAssistant/requirements.txt（排除投毒包 pylnk3，见 setup_m7.py）——
#   m7_venv 必须装两类，否则实时观测/任务中心瘫痪（见 CLAUDE.md）。
#
# 2026-08-11 版本升级（Python 3.14 兼容）：numpy 2.2.6/opencv 4.11/pillow 10.4
# 在 3.14 无预编译 wheel（会触发源码编译失败）——统一升到 3.14 有 wheel 的版本。
# 唯一环境 m7_venv（Python 3.14）。
numpy==2.5.2
pillow==12.3.0
PyAutoGUI==0.9.54
pywin32==312
PyYAML==6.0.3
requests==2.34.2
opencv-python==5.0.0.93
mss==10.2.0
ruamel.yaml==0.19.1
PySide6==6.11.1
PySide6-Fluent-Widgets==1.11.3
keyboard==0.13.5
pynput==1.8.2
